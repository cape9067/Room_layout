<div class="container-fluid">
  <div class="row">
    <div class="col-9 mt-4">
    	<%= render 'layouts/flash'%>
    	<div class="row">
    	<div class="col-md-6 h2 m-4 font-weight-bold">
        投稿一覧<small><small>(全<%= @rooms.total_count %>件)</small></small>
        </div>
        <div class="col-md-4 mt-4 mx-5 text-right pt-1">
	      <%= render 'sort' %>
	    </div>
</div>
            <hr>


	    <div class="row" id="room-index">
      	<%= render 'index', rooms: @rooms %>
      	  </div>

  		<div class="d-flex justify-content-center">
  		  <%= paginate @rooms, theme: 'bootstrap-5' %>
 		  </div>
		</div>

		<div class="col-md-3 p-4">
			<%= render 'side', categories: @categories, rooms: @latest_rooms %>
		</div>
	</div>
</div>