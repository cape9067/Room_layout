

<div class="col-md-9 mt-5">
	<div class="container">
	<div class="row">
<% @user.bookmarked_rooms.each do |room| %>
  <div class="card mb-4 shadow-sm mr-4">
  	<%= link_to public_room_path(room) do %>
      <%= image_tag room.image, size: "350x250", class:"p-1" %>
    <% end %>
    <div class="card-body">
      <div class="">
        <p class="card-title"><%= room.title %></p>
        <p class="btn btn-secondary btn-sm text-white">🏷️<%= room.category.name %></p>
        <small class="text-muted">投稿日：<%= room.created_at.strftime("%Y-%m-%d") %></small>
      </div>
     </div>
     <div class="card-footer">
      
     </div>

  </div>
<% end %>