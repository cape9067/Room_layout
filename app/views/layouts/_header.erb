<header class="border-bottom border-dark" style= "background-color: #8C5A2B;">

    <div class="container">

      <div class="col-5" style="right:100px;">
       rogo
      </div>


      <div class="col-7 mt-1">
        <div class="row">
          <div class="col-12">
            <div class="d-flex justify-content-end">
              <% if admin_signed_in? %>
                <%= render 'layouts/admin_header' %>
              <% elsif user_signed_in? %>
                <li class="d-flex mr-4">
                  <%= link_to public_user_path(current_user), class: "btn btn-primary" do %>
                    マイページ
                  <% end %>
                </li>
                <li class="d-flex mr-4">
                  <%= link_to  public_rooms_path, class: "btn btn-outline-secondary" do %>
                    投稿一覧
                  <% end %>
                </li>
                <li class="d-flex mr-4">
                  <%= link_to  public_user_bookmarks_path(current_user), class: "btn btn-outline-secondary" do %>
                    マイリスト
                  <% end %>
                </li>
                <li class="d-flex">
                  <%= link_to destroy_user_session_path, method: :delete, class: "btn btn-outline-secondary" do %>
                    ログアウト
                  <% end %>
                </li>
              <% else %>
                <li class="d-flex mr-4">
                  <%= link_to new_customer_registration_path,class: "btn btn-outline-secondary" do %>
                    新規登録
                  <% end %>
                </li>
                <li class="d-flex">
                  <%= link_to new_customer_session_path ,class: "btn btn-outline-secondary" do %>
                    ログイン
                  <% end %>
                </li>
              <% end %>
            </div>
          </div>
        </div>
      </div>
    </div>
  </div>
</header>
